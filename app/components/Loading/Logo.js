import styled from "styled-components";
import { fadedBlack, text } from "static/colors";

const Logo = styled.img`
  position: absolute;
  background-color: ${text};
  width: 5rem;
  padding: 0.5rem;
  border-radius: 50%;
`;

export default Logo;
